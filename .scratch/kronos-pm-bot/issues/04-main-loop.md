# 04 — 主循环编排 + 状态/日志 + 熔断联动

**What to build:** 15m 窗口主循环：时钟对齐窗口边界 → 更新数据（增量）→ 策略产出信号 → 市场发现 → 决策引擎 decide → 执行（真实或 dry-run）→ 持仓跟踪 → 日志记录；熔断联动：连亏/日亏触发 → 自动暂停 → 人工恢复入口；运行状态写入 status.json（含熔断状态）；每窗口每标的最多一注。

**Blocked by:** 02 — Kronos 策略（Binance 数据源 + CSV 滚动存储 + Kronos 推理）；03 — Polymarket 市场发现 + CLOB 执行

**Status:** resolved

- [ ] dry-run 模式全自动跑通一个完整窗口（日志完整、status.json 更新、持仓跟踪正确）
- [ ] 熔断联动：注入连亏/日亏场景触发暂停，人工恢复后继续按配置运行
- [ ] 每窗口每标的最多一注（含异常重跑保护）
- [ ] 数据源/预测/市场查询任一失败 → 跳过该窗口并记录，不崩溃
- [ ] 可长时间运行（无内存/状态泄漏迹象，窗口循环稳定）

## Comments

- 2026-08-14 实现完成：117 测试全绿（5d91a8e）
- 主循环：tick 编排（结算→熔断→窗口切换→市场发现→信号缓存→挂单检测→decide→执行→落盘）
- 真实 dry-run 冒烟通过（kronos-small + 增量拉取 + 信号 + 市场 + 盘口 + 模拟挂单）
- 熔断恢复：编辑 status.json 把 paused 改 false 即恢复（was_paused 自动清零计数）
- 实盘前提醒：钱包在 Polygon 上 USDC balance=0，需先充值到 Polygon 网络
