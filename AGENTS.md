# AGENTS.md

## Agent skills

### Issue tracker

Issues 以 markdown 文件存放在 `.scratch/<feature-slug>/` 下（本地 markdown）。见 `docs/agents/issue-tracker.md`。

### Triage labels

五个 canonical 角色使用默认 label 字符串。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
